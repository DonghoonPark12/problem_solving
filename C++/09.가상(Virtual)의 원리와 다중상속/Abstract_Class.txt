순수 가상함수 : 함수의 선언부만 있고 구현하지 않은 가상함수를 순수 가상함수라 한다. 


Q. 추상클래스 개념은 이해하겠는데 왜 Virtual로 선언을 할까?
A. Virtual로 선언되어야지만, 상속 받은 객체의 Virtual 함수가 호출되기 때문이다. 
   이는 곧 파생 클래스가 함수를 오버라이딩 하기를 강제(!)하는 역할을 한다.

   왜냐하면 파생 클래스의 Virtual 함수가 없으니 컴파일 에러가 나기 때문이다.(기본 클래스의 Virtual 함수가 호출되지 않는다!!)


